spring:
  flyway:
    enabled: true
    url: jdbc:mariadb://localhost:3306/SC_SCHEMA
    user: root
    #password in Vault

  datasource:
    url: jdbc:mariadb://localhost:3306/SC_SCHEMA
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    #password in Vault

  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
#    hikari:
#      auto-commit: true
#      driver-class-name: org.mariadb.jdbc.Driver
#      connection-test-query: SELECT 1
#      jdbc-url: jdbc:mariadb://localhost:3306/SC_SCHEMA
#      username: root

